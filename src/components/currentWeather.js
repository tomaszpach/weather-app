import React from 'react';

// Get data (props) from searchBar.js input
// Show temperature, pressure, wind, maybe some kind of icon
const currentWeather = () => {
    return (
        <div>currentWeather placeholder</div>
    )
};

export default currentWeather;